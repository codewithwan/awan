# awan ☁️

A tiny living character for your terminal — and a **personality layer** any
CLI can embed: `wait`, `ask`, `react`.

**Early development (v0.0.x).** Until the embed API lands, this crate
re-exports the engine (`awan_core`) and renderer (`awan_render`) for early
adopters. Expect breaking changes until v0.1.

See the [project README](https://github.com/codewithwan/awan) for the full
picture, the `awan` CLI, and how to contribute characters (plain TOML, zero
Rust).

## License

MIT OR Apache-2.0, at your option.
