# Node example

```sh
npm install
npm start
```

`awan.busy(...)` and `awan.react(...)` are plain function calls — the package
manages the character for you, nothing to spawn. See [index.js](index.js).
