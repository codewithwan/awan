# Shell example

```sh
./run.sh
```

No client to install — in the shell the `awan` command *is* the API. This is
also how the [`shell/awan.zsh`](../../shell/awan.zsh) hooks and the
`awan statusline` prompt integration work. See [run.sh](run.sh).
