# Python example

```sh
pip install -r requirements.txt
python main.py
```

`awan.busy(...)` and `awan.react(...)` are ordinary calls — the package drives
the character for you. See [main.py](main.py).
